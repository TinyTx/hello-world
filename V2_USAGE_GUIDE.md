# 跳水锦标赛照片下载器 V2 版本使用指南

## 🎯 V2版本专门解决的问题

根据您的反馈，V2版本专门针对以下问题进行了优化：

1. ✅ **照片检测问题**：改进了图片查找算法，确保能找到所有照片
2. ✅ **弹窗处理流程**：完整实现了"点击图片 → 查看原图 → 保存到本地"的流程
3. ✅ **保存按钮识别**：智能识别"保存到本地"按钮并自动点击
4. ✅ **弹窗关闭处理**：自动关闭查看完的图片弹窗，继续处理下一张

## 🚀 V2版本工作流程

### 完整的处理流程
```
1. 登录网站 → 2. 滚动加载所有内容 → 3. 找到所有图片
     ↓
4. 点击图片 → 5. 等待弹窗出现 → 6. 查找"查看原图"按钮
     ↓
7. 点击查看原图 → 8. 查找"保存到本地"按钮 → 9. 执行保存
     ↓
10. 关闭弹窗 → 11. 继续下一张图片
```

## 📋 使用步骤

### 快速开始
```bash
# 运行便捷脚本
./run_enhanced_downloader.sh

# 选择选项 2：V2版下载器
```

### 手动运行
```bash
# 直接运行V2版本
python3 diving_photos_downloader_v2.py
```

### 推荐流程
1. **先分析（可选）**：运行 `improved_page_analyzer.py` 了解网站结构
2. **正式下载**：运行 `diving_photos_downloader_v2.py`

## 🔧 V2版本特性

### 1. 智能图片检测
- 使用多种CSS选择器查找图片
- 自动过滤logo、图标等非比赛照片
- 基于图片源URL去重处理

### 2. 弹窗处理机制
- 自动检测弹窗出现
- 支持多种弹窗类型：modal, popup, overlay, dialog, lightbox
- 智能等待弹窗加载完成

### 3. 原图按钮识别
```javascript
// 支持的按钮类型
- 包含"查看原图"文字的按钮/链接
- 包含"原图"文字的元素
- class包含"original"、"full"、"hd"的元素
- title属性为"查看原图"的元素
```

### 4. 保存功能实现
- 自动识别"保存"、"下载"、"保存到本地"按钮
- 支持直接下载链接
- 如果没有保存按钮，自动提取当前显示的原图URL
- 多种保存方式备选

### 5. 弹窗关闭机制
- ESC键关闭
- 查找并点击关闭按钮（×、关闭）
- 点击背景区域关闭
- 多种方式确保弹窗正确关闭

## 📁 文件输出结构

```
diving_championship_photos_v2/
├── diving_photo_1642345678901.jpg    # 按时间戳命名的照片
├── diving_photo_1642345679234.jpg
├── diving_photo_1642345679567.jpg
├── ...
├── download_log_v2.txt               # V2版本的详细日志
└── progress_v2.json                  # V2版本的进度文件
```

## 📊 进度监控

### 实时日志显示
```
[2025-01-13 15:30:15] 滚动 50/300, 当前图片数量: 1250
[2025-01-13 15:30:18] 找到 2847 个有效图片元素
[2025-01-13 15:30:20] 处理图片 1/2847: https://example.com/photo1.jpg
[2025-01-13 15:30:22] 成功点击查看原图按钮
[2025-01-13 15:30:24] 点击了保存按钮
[2025-01-13 15:30:26] 直接下载成功: diving_photo_1642345678901.jpg (2048.5 KB)
[2025-01-13 15:30:27] 图片 1 处理成功
```

### 进度保存
- 每处理10张照片自动保存进度
- 支持断点续传，重新运行时跳过已处理的图片
- JSON格式记录所有下载状态

## 🛠️ 高级配置

### 调整滚动次数
```python
# 在 diving_photos_downloader_v2.py 中修改
max_attempts = 300  # 默认300次，可根据实际需要调整
```

### 调整处理间隔
```python
# 修改处理每张图片后的延迟
time.sleep(1)  # 默认1秒，可调整
```

### 启用无头模式
```python
# 在 setup_driver 方法中添加
chrome_options.add_argument('--headless')
```

## 🔍 故障排除

### 问题1：找不到图片
```
找到 0 个有效图片元素
```
**解决方案**：
1. 确保已登录并且可以看到照片
2. 先运行页面分析器检查图片元素
3. 等待页面完全加载后再开始

### 问题2：点击图片无弹窗
```
图片 X 点击失败或无弹窗
```
**解决方案**：
1. 检查图片是否可点击
2. 可能需要手动测试点击行为
3. 查看页面分析器的结果

### 问题3：找不到查看原图按钮
```
图片 X 未找到查看原图按钮
```
**解决方案**：
1. 运行页面分析器确认按钮文字
2. 可能需要修改按钮选择器
3. 手动测试确认按钮位置

### 问题4：保存失败
```
图片 X 下载失败
```
**解决方案**：
1. 检查网络连接
2. 查看日志中的具体错误信息
3. 可能需要手动测试保存流程

## 📞 使用建议

### 首次使用
1. **先分析**：运行 `improved_page_analyzer.py` 了解网站行为
2. **小批量测试**：先测试几张图片确认流程正确
3. **正式运行**：确认无误后运行完整下载

### 大批量下载
1. **稳定网络**：确保网络连接稳定
2. **充足空间**：准备足够的存储空间
3. **监控进度**：注意查看日志输出
4. **分批进行**：可以分多次运行，利用断点续传功能

### 优化建议
1. **关闭其他程序**：减少系统资源占用
2. **稳定电源**：确保电源稳定，避免中断
3. **定期检查**：定期查看下载进度和质量

---

**🎉 V2版本专门针对您提出的弹窗和保存流程问题进行了深度优化，现在应该能够正确处理完整的下载流程！**