# 跳水锦标赛照片下载器 - 增强版使用指南

## 🔧 最新修复 (v4.2)

### 紧急修复的问题
1. **Chrome浏览器启动失败**：
   - 移除了不兼容的 `loggingPrefs` 和 `perfLoggingPrefs` 选项
   - 增加了简化配置的备用启动方案
   - 改进了错误处理和重试机制
   - 现在应该可以在所有系统上正常启动Chrome

2. **滚动目标错误**：
   - 修正为专门滚动图片容器区域，而不是整个页面
   - 智能检测包含图片的容器元素
   - 增加了4种容器滚动策略
   - 提供备用的整页滚动方法

3. **网络监控简化**：
   - 移除了对Chrome性能日志的依赖
   - 使用简化的页面加载状态检测
   - 提高了兼容性和稳定性

### 新的工作流程
1. 启动脚本 → Chrome浏览器正常启动
2. 手动登录 → 输入 `y` 确认
3. 自动检测图片容器区域
4. 专门滚动图片区域加载更多内容
5. 下载所有检测到的照片

## 🔧 最新修复 (v4.1)

### 修复的问题
1. **登录输入无响应问题**：
   - 简化了登录确认流程
   - 增加了更清晰的提示信息
   - 支持多种输入格式 (`y`/`yes`/`Y`/`YES`)
   - 增加了错误处理和超时机制

2. **滚动方向问题**：
   - 修正为向上滚动加载内容（符合网站实际行为）
   - 从页面底部开始向上滚动
   - 增加了4种向上滚动策略
   - 智能检测到达页面顶部的情况

### 新的使用流程
1. 启动脚本后，会自动打开浏览器
2. 在浏览器中完成登录
3. 回到控制台，看到提示后输入 `y` 并按回车
4. 程序会自动从页面底部开始向上滚动加载所有内容
5. 加载完成后自动下载所有照片

## 🚀 新增功能特性

### 多维度动态加载检测
- **智能滚动策略**: 4种不同的滚动模式（平滑滚动、分步滚动、随机滚动、聚焦滚动）
- **网络请求监控**: 实时监控AJAX请求，确保所有动态内容加载完成
- **多重检测机制**: 同时检测页面高度、图片数量、网络活动等多个指标
- **强制触发加载**: 当常规方法失效时，自动触发所有可能的加载事件

### 全面的图片URL提取
- **5种提取方法**: 
  1. IMG元素属性提取
  2. 页面源代码正则匹配
  3. CSS背景图片提取
  4. JavaScript变量扫描
  5. Data属性深度挖掘
- **智能URL增强**: 自动移除尺寸限制，尝试获取最高质量版本
- **Srcset支持**: 完整支持响应式图片的srcset属性

### 增强的加载更多检测
- **扩展按钮识别**: 支持20+种不同的"加载更多"按钮样式
- **多种点击方式**: 自动尝试3种不同的点击方法
- **微赞平台优化**: 针对微赞平台的特殊按钮样式进行优化

### 网络性能监控
- **性能日志**: 启用Chrome性能日志，监控网络活动
- **内存管理**: 自动垃圾回收，防止内存泄漏
- **请求状态跟踪**: 实时跟踪网络请求状态

## 📋 系统要求

- Python 3.8+
- Chrome/Chromium 浏览器
- ChromeDriver（可自动管理）
- 至少4GB可用内存
- 稳定的网络连接

## 🛠️ 安装步骤

### 1. 快速启动（推荐）
```bash
chmod +x run_enhanced_downloader.sh
./run_enhanced_downloader.sh
```

### 2. 手动安装
```bash
# 创建虚拟环境
python3 -m venv venv
source venv/bin/activate

# 安装依赖
pip install -r requirements_enhanced.txt

# 运行脚本
python3 diving_photos_downloader_enhanced.py
```

## 🎯 使用流程

### 第一步：启动脚本
```bash
./run_enhanced_downloader.sh
```

### 第二步：浏览器登录
1. 脚本会自动打开Chrome浏览器
2. 访问目标网页
3. **手动完成登录**（重要！）
4. 在控制台输入 `y` 确认登录完成

### 第三步：自动处理
脚本将自动执行以下操作：
1. **智能滚动加载**: 使用4种滚动策略确保所有内容加载
2. **动态内容检测**: 监控页面变化和网络活动
3. **全面URL提取**: 使用5种方法提取所有图片链接
4. **原图质量增强**: 自动优化图片URL获取最高质量版本
5. **批量下载**: 按日期分类下载所有照片

## ⚙️ 高级配置

### 修改下载设置
编辑 `diving_photos_downloader_enhanced.py` 文件：

```python
# 修改下载目录
download_dir = "my_photos"

# 修改最大滚动次数
max_attempts = 500

# 修改稳定检测阈值
stable_count_threshold = 10
```

### 启用无头模式
取消注释以下行：
```