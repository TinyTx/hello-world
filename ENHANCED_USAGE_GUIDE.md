# 跳水锦标赛照片下载器增强版使用指南

## 🚀 新功能概述

根据您提出的问题，我们已经创建了全新的增强版脚本，专门解决以下问题：

✅ **1. 登录功能**：脚本会等待您手动登录后再开始下载  
✅ **2. 原图检测**：智能识别并下载最高质量的原图  
✅ **3. 完整下载**：优化以支持1万多张照片的完整下载  
✅ **4. 断点续传**：支持中断后继续下载  
✅ **5. 进度保存**：自动保存下载进度  

## 📋 使用步骤

### 第一步：环境检查
```bash
# 运行环境检查和安装
sudo ./setup_environment.sh
```

### 第二步：分析页面结构（推荐）
```bash
# 先运行页面分析器了解网站结构
./run_enhanced_downloader.sh
# 选择选项 1
```

这一步会：
- 分析网页的图片显示机制
- 找到"查看原图"按钮的位置
- 了解原图获取方式
- 为正式下载做准备

### 第三步：正式下载
```bash
# 运行增强版下载器
./run_enhanced_downloader.sh
# 选择选项 2
```

## 🔐 登录流程

1. **启动脚本**：脚本会自动打开浏览器并访问网页
2. **等待加载**：等待5秒让页面完全加载
3. **手动登录**：
   - 在浏览器中完成登录操作
   - 确保能够看到需要登录才能查看的原图
4. **确认登录**：在控制台输入 `y` 确认登录完成
5. **开始分析**：脚本开始分析页面并提取照片链接

## 📸 原图获取机制

增强版脚本采用多种方法确保获取原图：

### 方法1：属性检查
- 检查 `data-original`、`data-src`、`data-large` 等属性
- 查找高分辨率图片链接

### 方法2：父元素分析
- 检查图片父元素的链接
- 提取可能的原图URL

### 方法3：模拟点击（核心功能）
- 自动点击图片元素
- 检测是否打开新窗口或弹窗
- 查找"查看原图"按钮
- 获取高清原图链接

### 方法4：URL优化
- 移除尺寸限制参数（如 `?w=300&h=200`）
- 测试不同的高质量URL后缀
- 选择文件大小最大的版本

## 🔄 完整下载保障

### 智能滚动机制
- 最多尝试200次滚动（针对1万多张照片）
- 自动点击"加载更多"按钮
- 监控页面高度和图片数量变化
- 连续5次无新内容后停止

### 进度保存系统
```
diving_championship_photos_original/
├── progress.json        # 保存下载进度
├── download_log.txt     # 详细日志
└── 各日期文件夹...
```

### 断点续传功能
- 自动加载之前的下载记录
- 跳过已下载的文件
- 支持多次运行直到完成

## 📊 监控和统计

### 实时信息显示
```
[2025-01-13 14:30:15] 滚动 50/200, 当前图片数量: 1250
[2025-01-13 14:30:18] 点击了加载更多按钮
[2025-01-13 14:30:25] 处理进度: 100/1500 (总进度: 2500/10000)
[2025-01-13 14:30:26] 已下载: photo_1234.jpg (1024.5 KB)
```

### 统计信息
- 找到的图片总数
- 已下载的文件数量
- 按日期分类的统计
- 跳过的重复文件数

## 🛠️ 高级配置

### 无头模式（服务器环境）
编辑 `diving_photos_downloader_enhanced.py`：
```python
# 找到这一行并取消注释
chrome_options.add_argument('--headless')
```

### 下载速度调整
```python
# 修改下载间隔（当前为0.5秒）
time.sleep(0.5)  # 可调整为更大或更小的值
```

### 滚动次数调整
```python
# 修改最大滚动次数（当前为200次）
max_attempts = 200  # 可根据实际需要调整
```

## 🔍 问题排查

### 如果下载的不是原图
1. 先运行 `analyze_page_structure.py` 分析页面
2. 检查控制台输出，确认找到了"查看原图"按钮
3. 手动测试点击图片是否能显示原图选项

### 如果下载数量不完整
1. 检查 `download_log.txt` 中的错误信息
2. 查看 `progress.json` 中的已下载列表
3. 重新运行脚本，会自动继续未完成的部分

### 如果登录失败
1. 确保网络连接正常
2. 检查登录凭据是否正确
3. 查看浏览器控制台是否有错误信息

## 📁 输出文件说明

```
diving_championship_photos_original/
├── 2025-01-15/              # 按比赛日期分类
│   ├── photo_001.jpg        # 高质量原图文件
│   ├── photo_002.jpg
│   └── ...
├── 2025-01-16/
├── 2025-01-17_unknown/      # 无法识别日期的照片
├── download_log.txt         # 详细操作日志
└── progress.json           # 进度保存文件（JSON格式）
```

### progress.json 格式
```json
{
  "downloaded_files": ["url1", "url2", ...],
  "downloaded_hashes": ["hash1", "hash2", ...],
  "last_update": "2025-01-13T14:30:25"
}
```

## ⚡ 性能优化

### 对于1万多张照片的建议
1. **分批下载**：可以分多次运行，每次下载几千张
2. **网络稳定**：确保网络连接稳定，避免频繁中断
3. **存储空间**：确保有足够的磁盘空间（建议至少50GB）
4. **内存使用**：监控内存使用情况，必要时重启脚本

### 下载时间估算
- 平均每张照片：2-5MB
- 下载速度：约10-20张/分钟（取决于网络和服务器响应）
- 预计总时间：8-16小时（10000张照片）

## 🔒 注意事项

1. **版权遵守**：下载的照片仅供个人学习使用
2. **网络礼仪**：脚本已设置合理间隔，请勿修改得过于频繁
3. **存储管理**：定期清理重复或低质量的文件
4. **备份建议**：重要照片请及时备份到其他存储设备

## 📞 技术支持

如果在使用过程中遇到问题：

1. **查看日志**：检查 `download_log.txt` 中的错误信息
2. **重新运行**：大多数问题可通过重新运行脚本解决
3. **调试模式**：使用 `analyze_page_structure.py` 进行问题诊断

---

**🎉 现在您可以开始使用增强版下载器，享受完整的1万多张高质量跳水锦标赛照片下载体验！**